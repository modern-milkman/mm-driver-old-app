import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  map: {
    position: 'absolute',
    width: '100%',
    top: 0,
    left: 0,
    zIndex: 1
  }
});

export default styles;
