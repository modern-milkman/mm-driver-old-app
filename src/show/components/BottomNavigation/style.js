import { StyleSheet } from 'react-native';

// branding
const styles = StyleSheet.create({
  container: { borderColor: '#cccccc', borderTopWidth: 1, padding: 18 },
  upNext: { backgroundColor: '#EAF1F8', borderRadius: 10 }
});

export default styles;
